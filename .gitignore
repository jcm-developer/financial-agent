# Credenciales. NUNCA subir.
.env

# Base de datos y sus ficheros auxiliares de WAL.
data/
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
spike_*.jsonl
backup/

# Frontend (F4). `app/src/api/types.ts` SI se sube: lo genera
# tools/gen_api_types.py y tenerlo en el repositorio permite compilar el
# frontend sin ejecutar Python antes.
node_modules/
app/dist/
.vite/

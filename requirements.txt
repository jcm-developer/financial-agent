# Todas las dependencias. El experimento funciona con una sola clave de API, la
# del modelo: los datos son de Yahoo (sin clave) y el broker es el simulador.

# Datos de mercado (arrastra pandas y numpy).
yfinance>=0.2.50
# Cliente HTTP para NVIDIA NIM. Tambien es lo que usa el TestClient de la API.
httpx>=0.27.0
python-dotenv>=1.0.1

# API (F3). Sustituyo al `http.server` con el que se servia el dashboard viejo:
# con ~20 endpoints, escrituras y SSE, enrutar y validar a mano dejaba de
# compensar. Dentro de Docker las dependencias no cuestan nada.
fastapi>=0.115.0
uvicorn>=0.30.0
# zoneinfo necesita la base de datos de zonas horarias: las imagenes slim de
# Python y Windows no la traen del sistema.
tzdata>=2024.1
pytest>=8.3.0
